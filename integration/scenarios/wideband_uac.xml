<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Scenario 3 UAC: place a call to sip:wideband@, play the two-tone fixture, hang up.

  Byte-for-byte the scenario 1 dialogue, and deliberately so. The caller is an ordinary 8 kHz
  G.711 A-law phone in both runs and offers exactly the same SDP; the only difference is on the
  other side of the proxy, where the media profile adds ws_sample_rate: 16000 and the engine
  negotiates a 16 kHz WebSocket with the bot. Two runs that differ in one negotiated number are
  comparable; two scenarios that differ in their caller are not.

  The one SIP-level assertion is the same one: the 200 OK's c= line has to be the engine's own
  relay address, because RTP sent anywhere else fails every audio assertion downstream for a
  reason that has nothing to do with audio.

  A-law is offered because the fixture is A-law, and the wire rate the engine negotiates with the
  bot has no bearing on that: the caller's codec and the WebSocket's rate are independent, which
  is the whole point of the scenario. ptime is 20 to match the fixture's 160-byte payloads.

  The IPv4 literal in the ereg is the engine's address on the harness network. It is duplicated
  from docker-compose.yaml on purpose: an assertion that reads its expected value out of the
  same variable as the thing under test asserts nothing.
-->
<scenario name="pipecat-siphon wideband UAC">

  <send retrans="500">
    <![CDATA[
INVITE sip:wideband@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550100@harness.example>;tag=[pid]SIPpTag00[call_number]
To: <sip:wideband@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:+15550100@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
User-Agent: SIPp/pipecat-siphon-harness
Content-Type: application/sdp
Content-Length: [len]

v=0
o=caller 1 1 IN IP[local_ip_type] [local_ip]
s=-
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 8 101
a=rtpmap:8 PCMA/8000
a=rtpmap:101 telephone-event/8000
a=fmtp:101 0-15
a=ptime:20
a=sendrecv
]]>
  </send>

  <recv response="100" optional="true" />

  <recv response="200" rtd="true" crlf="true" rrs="true">
    <action>
      <ereg regexp="c=IN IP4 172\.28\.7\.10"
            search_in="body"
            check_it="true"
            assign_to="anchored" />
      <log message="answer anchored on the engine: [$anchored]" />
      <ereg regexp="a=rtpmap:8 PCMA/8000"
            search_in="body"
            check_it="true"
            assign_to="codec" />
      <log message="answer codec: [$codec]" />
    </action>
  </recv>

  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550100@harness.example>;tag=[pid]SIPpTag00[call_number]
To: <sip:wideband@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- 200 packets at 20 ms: 4.0 s of the 500 Hz + 1300 Hz fixture. -->
  <nop>
    <action>
      <exec play_pcap_audio="/harness/artifacts/wideband.pcap" />
    </action>
  </nop>

  <!-- Half a second past the end of the fixture, so the last echoed frames have made it back
       through the bot and into the capture before the call goes away. -->
  <pause milliseconds="4500" />

  <send retrans="500">
    <![CDATA[
BYE [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:+15550100@harness.example>;tag=[pid]SIPpTag00[call_number]
To: <sip:wideband@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
[routes]
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <recv response="200" rtd="true" />

</scenario>
